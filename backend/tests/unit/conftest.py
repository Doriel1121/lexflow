# Unit test conftest — intentionally empty.
# This overrides the parent conftest.py which imports the full app
# (and all its heavy dependencies like spacy, etc.).
# Unit tests should mock their heavy dependencies instead.
